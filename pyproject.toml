[project]
name = "advent-of-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "semino", email = "semino@posteo.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
advent-of-code = "advent_of_code.cli:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
